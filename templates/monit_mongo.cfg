check host mongodb_{{mongo_port}} with address "{{inventory_hostname}}"
        if failed port {{mongo_port}} protocol http request "/isMaster?text=1" then exec "{{mongo_installation_dir}}/bin/mongo --config {{mongo_configfile}}"
